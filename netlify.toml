[build]
  command = "cd frontend && npm ci && CI=false npm run build"
  publish = "frontend/build"

[build.environment]
  CI = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
